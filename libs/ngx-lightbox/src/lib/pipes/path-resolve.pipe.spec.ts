import { PathResolvePipe } from './path-resolve.pipe';

describe('PathResolvePipe', () => {
  it('create an instance', () => {
    const pipe = new PathResolvePipe();
    expect(pipe).toBeTruthy();
  });
});
